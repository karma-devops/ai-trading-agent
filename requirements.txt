# ============================================================================
# HyperLiquid Trading Agent - Private Deployment Dependencies
# Trimmed to core providers only: OpenAI-compatible, Ollama, OpenRouter,
# Anthropic, Google Gemini. Others can be pip installed manually if needed.
# ============================================================================

# Web framework
flask==3.0.0
flask_cors==5.0.0
python-dotenv==1.2.1
requests==2.32.5
urllib3==2.6.2
werkzeug==3.0.0

# Ethereum / HyperLiquid
cytoolz==1.1.0
eth-account==0.13.7
eth-hash==0.7.1
eth-keyfile==0.8.1
eth-keys==0.7.0
eth-rlp==2.2.0
eth-typing==5.2.1
eth-utils==5.3.1
hexbytes==1.3.1
hyperliquid-python-sdk>=0.24.0
pyasn1==0.6.1
pyasn1_modules==0.4.2
pycryptodome==3.23.0
rlp==4.1.0

# AI providers (core only — pip install others manually if needed)
openai==2.14.0
anthropic==0.75.0
google-generativeai==0.8.6

# Data & analysis
numpy>=2.2.6
pandas==2.3.3
pandas-ta==0.4.71b0
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3

# Utils
rich==14.2.0
six==1.17.0
termcolor==3.2.0
tqdm==4.67.1

# Async / WebSocket
anyio==4.12.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
sniffio==1.3.1
websocket-client==1.9.0
websockets==15.0

# Production server
gunicorn==23.0.0

# Typing
typing_extensions==4.15.0

# Removed (install manually if needed):
# groq — pip install groq
# ollamafreeapi — pip install ollamafreeapi
# ipython — pip install ipython (dev only)
# matplotlib-inline — dev only
# cachetools, distro, jiter, regex — transitive deps, auto-installed